# Local-only Claude Code settings (machine-specific permissions)
.claude/settings.local.json

# User's personal screenshots/videos (contain catch locations) — never publish
samples/

# OS noise
Thumbs.db
.DS_Store
